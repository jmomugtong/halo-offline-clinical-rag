\begin{tikzpicture}[
  node distance=1.0cm and 1.6cm,
  startbox/.style={rectangle, draw, rounded corners=4pt, fill=gray!10,
                   align=center, font=\small, inner sep=6pt,
                   minimum width=2.8cm},
  diamond/.style={diamond, draw, fill=yellow!15, align=center,
                  font=\scriptsize, inner sep=3pt,
                  aspect=2.2, minimum width=3.8cm, minimum height=1.0cm},
  parabox/.style={rectangle, draw, rounded corners=4pt, fill=green!15,
                  align=center, font=\scriptsize, inner sep=6pt,
                  minimum width=2.8cm, minimum height=1.0cm},
  contrbox/.style={rectangle, draw, rounded corners=4pt, fill=red!15,
                   align=center, font=\scriptsize, inner sep=6pt,
                   minimum width=2.8cm, minimum height=1.0cm},
  arr/.style={-Stealth, thick},
  lbl/.style={font=\scriptsize\bfseries},
]

\node[startbox] (query) {Query $q \in \mathcal{Q}$};

\node[diamond, below=0.9cm of query] (dec)
    {BioMistral-7B correct\\zero-shot?};

\node[parabox, below left=1.1cm and 1.4cm of dec] (para)
    {Para-parametric $\mathcal{Q}_p$\\(retrieval benefit\\expected low)};

\node[contrbox, below right=1.1cm and 1.4cm of dec] (contra)
    {Contra-parametric $\mathcal{Q}_c$\\(retrieval benefit\\expected high)};

\draw[arr] (query) -- (dec);
\draw[arr] (dec) -| node[near start, above, lbl, text=green!60!black] {Yes} (para);
\draw[arr] (dec) -| node[near start, above, lbl, text=red!70!black]   {No}  (contra);

\end{tikzpicture}
